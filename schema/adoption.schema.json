{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "play-nice/adoption-v1",
  "title": "Play-Nice Project Adoption Manifest",
  "description": "How a project consumes the shared contract library. Projects consume; they do not fork.",
  "type": "object",
  "required": ["schema", "source"],
  "additionalProperties": false,
  "properties": {
    "schema": {
      "const": "play-nice/adoption-v1",
      "description": "Exact schema marker. Unknown values must fail clearly, never be silently reinterpreted."
    },
    "project": {
      "type": "string",
      "maxLength": 64,
      "description": "Optional project name for humans."
    },
    "source": {
      "type": "object",
      "required": ["repository", "revision"],
      "additionalProperties": false,
      "properties": {
        "repository": {
          "type": "string",
          "description": "Repository identifier the library is consumed from, e.g. Rylee-Bee/play-nice-contracts."
        },
        "revision": {
          "type": "string",
          "minLength": 7,
          "maxLength": 64,
          "description": "Pinned revision (commit SHA preferred). Drift from this pin is reported as stale."
        }
      }
    },
    "profile": {
      "type": ["string", "null"],
      "description": "Optional profile name layered above universal contracts. A profile may raise but never lower universal floors."
    },
    "always": {
      "type": "array",
      "items": { "$ref": "#/$defs/contract_id" },
      "description": "Contracts loaded for every task in this project."
    },
    "triggers": {
      "type": "object",
      "description": "Surface tag -> contracts loaded when a task matches that surface.",
      "additionalProperties": {
        "type": "array",
        "items": { "$ref": "#/$defs/contract_id" }
      }
    },
    "requirements": {
      "type": "object",
      "description": "Optional machine-checkable acceptance rules the project adds on top of contracts.",
      "additionalProperties": true
    },
    "freshness": {
      "type": "object",
      "required": ["policy"],
      "additionalProperties": false,
      "description": "Optional remote-freshness policy. Absent means {policy: pinned, ref: main, update: review} (legacy behavior). policy: pinned keeps the reviewed revision authoritative; policy: require-current makes a verified authoritative remote revision a prerequisite of CONTRACT COMMITMENT: ACTIVE (fail closed on UNKNOWN/UNREACHABLE).",
      "properties": {
        "policy": {
          "enum": ["pinned", "require-current"],
          "description": "pinned: existing behavior — the reviewed revision is authoritative for the consumer. require-current: the configured authoritative remote MUST be checked (git ls-remote) before mutating work."
        },
        "ref": {
          "type": "string",
          "default": "main",
          "description": "Authoritative branch the remote is checked against (branch name, not full ref). Ignored under policy: pinned."
        },
        "update": {
          "enum": ["review", "automatic"],
          "default": "review",
          "description": "review: a newer remote blocks mutation pending explicit adoption review. automatic: the pin may be refreshed automatically, but a fresh resolve/read/attest/commit cycle is still mandatory."
        }
      }
    },
    "notes": {
      "type": "string",
      "maxLength": 2000,
      "description": "Free-form context for humans and agents."
    }
  },
  "$defs": {
    "contract_id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "maxLength": 64
    }
  }
}